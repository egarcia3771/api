# Vercel KV 配置示例
# 部署到 Vercel 后,这些环境变量会自动配置,无需手动设置

# KV_REST_API_URL=your_kv_rest_api_url
# KV_REST_API_TOKEN=your_kv_rest_api_token
# KV_REST_API_READ_ONLY_TOKEN=your_kv_rest_api_read_only_token

# 注意: 
# 1. 部署到 Vercel 后,在项目的 Storage 标签页创建 KV 数据库
# 2. Vercel 会自动将这些环境变量注入到你的项目中
# 3. 本地开发时,可以从 Vercel Dashboard 复制这些值到 .env 文件
